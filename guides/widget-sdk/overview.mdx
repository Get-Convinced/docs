---
title: 'Widget SDK overview'
description: 'Add one Convinced conversation to any website and render it with your own UI.'
icon: 'layer-group'
---

`@convinced/widget-sdk` 0.1.7 has a headless core. It manages a Convinced conversation, optional speech I/O, host tools, and session lifecycle without requiring React. Build every visual surface in your application, or opt into the package's `mountConvincedWidget()` convenience renderer.

Version 0.1.7 lets the Convinced Luna agent carry a request through knowledge and browser tools while Live handles speech. Luna can supply a short spoken briefing alongside the full written answer. The SDK stops a superseded delegated turn before starting later browser actions. See [Optional Live voice](/guides/widget-sdk/voice).

The only agent identifier exposed in browser configuration is `agentId`, the public ID of a Convinced `AgentDeployment`. Do not put an OpenAI key, a model-provider ID, or any other provider credential in browser code.

## Availability and validation status

Reviewed September 24, 2026. The npm `latest` release is **0.1.7**. A local build with that same version label can contain unpublished changes; the version label alone does not establish which behavior is deployed.

| Capability | Published 0.1.7 | Current local development |
| --- | --- | --- |
| Headless chat, optional managed UI, identity/forms, slides and WebMCP | Available; host integration and backend configuration still required | Preserved |
| Live voice and short final voice briefings | Available, subject to real-browser acceptance | Further conversation and feedback changes under test |
| Incremental verified findings during delegated work | Do not assume the local streaming changes are included | Implemented; requires matching SDK and backend |
| `client.describeScreen()` | Not part of the published package | Experimental; matching authenticated backend and visitor-approved tab capture required |
| Independent overlapping transcript buffers, replayed-event protection, correlated mute acknowledgements | Local fixes are not included in the immutable npm package | Automated checks pass; not an acoustic acceptance result |

<Warning>
  Voice release acceptance is blocked by an unresolved report of false caller transcriptions during silence, including while muted, with speech interruptions. Ending voice stopped the reported behavior. A short digital-silence probe did not reproduce the transcripts. Local transcript and mute-acknowledgement fixes do not establish that this issue is resolved. Keep text available; see [voice troubleshooting](/guides/widget-sdk/voice#known-issue-false-input-during-silence).
</Warning>

No new SDK version or production rollout is implied by these local changes. Use the [migration guide](/guides/widget-sdk/migration) to distinguish released upgrades from pending work.

## One conversation, two inputs

Text and speech share one Convinced session. `gpt-6-luna` handles typed input and requests that need grounded organization knowledge, page context, slides, or tools.

- `client.sendMessage()` sends typed input directly to the `gpt-6-luna` conversation.
- Optional `gpt-live-1` provides full-duplex speech and answers greetings and conversational clarifications directly.
- Live delegates requests that need organization knowledge, page context, slides, or tools to `gpt-6-luna`.
- Completed Live-native user/assistant pairs enter `client.state.messages`, so typed and delegated Luna requests share the available conversation history.
- When voice is active, a typed turn still enters the same conversation and its answer is spoken. A newly submitted typed turn supersedes an unfinished delegated voice turn.
- Disabling or ending voice leaves chat active.
- A chat-only integration never creates an OpenAI Live session.

Convinced's backend owns OpenAI credentials, model selection, prompts, knowledge, and the delegation bridge.

```mermaid
flowchart LR
  UI[Host website UI] --> SDK[Widget SDK]
  SDK --> Chat[Grounded GPT-6 Luna work]
  SDK -. optional audio .-> Live[gpt-live-1]
  Live -->|grounded requests and recent spoken context| Chat
  Chat --> Tools[Signed tool continuation]
  Tools --> Registry[Shared ClientToolRegistry]
  Registry --> UI
```

## What the host owns

In a headless integration, the host website decides how to present SDK state. It can render a floating panel, an inline guide, a full-page experience, or no persistent surface at all. The same applies to slide placement, transitions, consent UI, form fields, and error states.

The optional managed renderer supplies a functional Shadow DOM interface, voice and chat controls, identity UI, and deployment-driven presentation. Pass the same client and Live controller to it:

```ts
const live = client.createLiveController()
const widget = mountConvincedWidget({ client, voice: live })
```

The renderer is a convenience layer over the same headless APIs. Use a custom renderer when the host must own exact layout, theme, slide behavior, or form behavior.

Use one `ClientToolRegistry` for chat and voice. The Convinced backend pauses a Luna turn before a browser tool, keeps the agent's run state on the server, and returns the browser an opaque signed continuation. The SDK validates the requested call against the local registry and host policy, executes the handler, and returns the result. The backend checks the signed call and one-use continuation before Luna resumes with that observation. WebMCP tools use the same registry and policy.

## Start here

<CardGroup cols={2}>
  <Card title="Quickstart" icon="bolt" href="/guides/widget-sdk/quickstart">Create a chat-only conversation, then opt in to voice.</Card>
  <Card title="Custom UI" icon="palette" href="/guides/widget-sdk/custom-ui">Render state and content in your framework.</Card>
  <Card title="Voice" icon="microphone-lines" href="/guides/widget-sdk/voice">Understand Live delegation and channel behavior.</Card>
  <Card title="SDK API map" icon="code" href="/guides/widget-sdk/api-reference">Find the public 0.1.7 surface.</Card>
</CardGroup>
