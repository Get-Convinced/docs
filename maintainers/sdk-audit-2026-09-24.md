# SDK documentation audit — September 24, 2026

Reviewed all 20 pages under `guides/widget-sdk` against the current SDK and backend worktrees and the published npm version (`latest` = 0.1.7). Updated 16 pages; prompt administration, campaigns, managed iframe and host-tool contracts retain their existing interfaces. No new package version or hosted rollout is asserted.

The overview owns the shared availability table. Voice documents the unresolved false-input report and distinguishes automated protocol checks from acoustic acceptance. Migration identifies local changes as unreleased. Identity clarifies session context versus durable visitor linkage. Media and page-aware guides distinguish host-specific page visuals from removal of SDK media. Screenshot documentation is explicitly experimental and requires visitor-approved capture plus matching backend support.

The public handoff is reusable. Customer deployment identifiers and rollout details belong in the customer's implementation material, not this public repository.

Validation:

- Pinned Mintlify CLI 4.2.893: `validate` passed.
- Pinned Mintlify CLI 4.2.893: `broken-links` passed.
- Browser preview: all 20 sidebar SDK pages rendered with their expected page titles; overview availability table and warning rendered.
- No complete code examples were added or changed; new API mentions were checked against local declarations/implementation and labeled unreleased where necessary.
- Production publication and live-site verification have not occurred for this change.
